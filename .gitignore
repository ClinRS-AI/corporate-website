.next/
out/
node_modules/
.DS_Store

# Windows / WSL junk (thumbnails, folder prefs, download zone metadata)
Thumbs.db
Desktop.ini
*Zone.Identifier

# Local env files
.env
.env.local
.env.*.local

# Logs
*.log

# TypeScript incremental build cache
*.tsbuildinfo

# Claude Code — local overrides (machine-specific; may include secrets)
.claude/settings.local.json

# Vercel CLI (project link, local project settings)
.vercel

# Tooling caches
.eslintcache

# Test coverage (when you add a test runner)
coverage/

# Playwright (local HTML report, traces, failures — can include page snapshots)
playwright-report/
test-results/

# Local TLS / key material (never commit real secrets)
*.pem
